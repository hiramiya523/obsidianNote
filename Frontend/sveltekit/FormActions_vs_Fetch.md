# SvelteKit Form Actions vs fetch 実装方針とアーキテクチャ選定

タグ: #sveltekit #form-actions #fetch #laravel #bff #architecture #best-practices

---

## 📋 目次

- [[#結論：迷ったらこう選ぶ]]
- [[#1. 核心的違い：「宣言的」vs「命令的」]]
- [[#2. 実装比較：Form Actions vs Fetch]]
- [[#3. Fetch (Client Side) の使いどころ]]
- [[#4. 用語整理・アーキテクチャ]]
- [[#5. 経営・マネジメント視点での選定理由]]
- [[#6. 3年・5年・10年先を見据えたロードマップ]]

---

## 結論：迷ったらこう選ぶ

SvelteKit 2.x + Svelte 5 + Laravel 11 環境における最適な選択基準は以下の通りです。

| シチュエーション | 推奨手段 | 理由 |
| :--- | :--- | :--- |
| **データの更新（Mutation）**<br>ログイン、保存、登録、設定変更など | **Form Actions**<br>(+ `use:enhance`) | ・**自動再検証 (Automatic Revalidation)** により、更新後のデータ再取得が不要。 ・ローディングやエラー状態の管理がフレームワークによって自動化される。 ・JSなしでも動く堅牢性（プログレッシブエンハンスメント）。 |
| **高度なインタラクション**<br>リアルタイム検索、無限スクロール、「いいね」ボタン | **fetch** | ・ページ遷移を伴わない細かいUI更新に向く。 ・イベント（`oninput`など）から自由に呼べる。 |
| **バックエンドとの通信** | **SvelteKit経由 (BFF)** | ・ブラウザからLaravelを直接叩かず、SvelteKitサーバーを経由させることでセキュリティと柔軟性を担保する。 |

**合言葉:**
> **「フォームは Form Actions、インタラクションは Fetch。通信は BFF (SvelteKit) を通す。」**

---

## 1. 核心的違い：「宣言的」vs「命令的」

なぜ Form Actions が推奨されるのか？ その理由は「Laravelと通信できるか」ではなく、**「SvelteKitのライフサイクルに乗っかるか、自前で管理するか」** という点にあります。

### Form Actions (宣言的)

フレームワークが面倒な処理を肩代わりします。

1. **データ整合性の自動化**: Action成功後、SvelteKitは**自動的に `load` 関数を再実行**します。リストの更新処理などを書く必要がありません。
2. **エラーと状態管理**: サーバーから `fail()` を返すだけで、クライアントの `form` プロパティにエラーや入力値が注入されます。
3. **競合状態の回避**: `use:enhance` がリクエストのライフサイクルを適切に管理します。

### Fetch (命令的)

エンジニアが全て手動で制御する必要があります。

1. **手動再検証**: `fetch` 完了後、`invalidateAll()` を呼ばないと画面は古いままです（バグの温床）。
2. **ボイラープレート**: ローディングフラグ (`isLoading`)、エラーメッセージ、バリデーションエラーのマッピングなどを全て手書きする必要があります。

---

## 2. 実装比較：Form Actions vs Fetch

「ユーザーを作成して一覧を更新する」というシナリオで比較します。

### パターンA：Form Actions (推奨)

コード量が少なく、バグが入り込む余地が少ない実装です。

**Server (`+page.server.ts`)**
```typescript
import { fail } from '@sveltejs/kit';
import type { Actions } from './$types';

export const actions: Actions = {
    create: async ({ request, fetch }) => {
        // ... Laravel API へ POST ...
        // const res = await fetch(...);

        if (!res.ok) {
            const errorBody = await res.json();
            // failを返すだけで、クライアントの `form` 変数が更新される
            return fail(422, {
                errors: errorBody.errors,
                data: { name } // 入力値を送り返してフォームに残す
            });
        }
        
        // returnした時点でSvelteKitは「成功」とみなし、load関数を自動再実行する
        return { success: true };
    }
};
```

**Client (`+page.svelte`)**
```svelte
<script lang="ts">
    import { enhance } from '$app/forms';
    
    // form: Actionからの戻り値（エラーや入力値）が自動で入る
    // data: load関数から来た最新データ（Action成功後に自動更新される）
    let { data, form } = $props();
    
    let isLoading = $state(false);
</script>

<!-- use:enhance でリクエストサイクルをSvelteKitに委譲 -->
<form 
    method="POST" 
    action="?/create" 
    use:enhance={() => {
        isLoading = true; // 送信開始
        
        return async ({ update }) => {
            isLoading = false; // 完了
            // update() が以下を全自動で実行：
            // 1. バリデーションエラーがあれば `form` 変数を更新
            // 2. 成功すれば `load` 関数を再実行（リストの最新化）
            // 3. フォームのリセット（オプションで制御可）
            await update();
        };
    }}
>
    <!-- 失敗時に値を戻すのも form.data で自動 -->
    <input name="name" value={form?.data?.name ?? ''} disabled={isLoading} />
    {#if form?.errors?.name}<p class="error">{form.errors.name}</p>{/if}

    <button disabled={isLoading}>
        {isLoading ? '保存中...' : '保存'}
    </button>
</form>

<ul>{#each data.users as user}<li>{user.name}</li>{/each}</ul>
```

### パターンB：Fetch (非推奨・手動管理)

同じことをFetchでやろうとすると、状態管理と再検証の手続きが大量に発生します。

**Client (`+page.svelte`) Only**
```svelte
<script lang="ts">
    import { invalidateAll } from '$app/navigation';
    
    let { data } = $props();
    
    // 手動管理が必要な状態（ボイラープレート）
    let isLoading = $state(false);
    let errorMessage = $state('');
    let errors = $state({}); 
    let name = $state('');

    async function handleSubmit(e: Event) {
        e.preventDefault();
        
        isLoading = true;
        errorMessage = '';
        errors = {};

        try {
            // APIルート または Laravel を直接叩く
            const res = await fetch('/api/users', {
                method: 'POST',
                body: JSON.stringify({ name })
            });

            const result = await res.json();

            if (!res.ok) {
                // エラーハンドリング（手動）
                if (res.status === 422) {
                    errors = result.errors;
                } else {
                    errorMessage = 'システムエラー';
                }
                return;
            }

            // 成功時の処理（手動）
            name = '';
            
            // 【最重要】画面データの最新化（手動）
            // これを忘れると「保存したのにリストに出ない」バグになる
            await invalidateAll(); 

        } catch (e) {
            errorMessage = '通信エラー';
        } finally {
            isLoading = false;
        }
    }
</script>

<form onsubmit={handleSubmit}>
    <input bind:value={name} disabled={isLoading} />
    <!-- エラー表示も自前で管理したstateを参照 -->
    {#if errors.name}<p>{errors.name}</p>{/if}
    
    <button disabled={isLoading}>
        {isLoading ? '保存中...' : '保存'}
    </button>
</form>
```

---

## 3. Fetch (Client Side) の使いどころ

更新系以外、特に「フォーム」という概念に当てはまらない高度なインタラクションには Fetch が適しています。

**適用シーン:**

- リアルタイム検索（インクリメンタルサーチ）
- 無限スクロール
- 「いいね」ボタン（楽観的UI更新）

### 実装例：リアルタイム検索 (Svelte 5 Runes)

```svelte
<script lang="ts">
    // ここではBFFパターン推奨のため、SvelteKitのAPIルートを叩く想定
    
    let query = $state('');
    let results = $state<{ id: number; title: string }[]>([]);
    let loading = $state(false);
    
    // デバウンス処理などは省略
    async function handleInput(e: Event) {
        const target = e.target as HTMLInputElement;
        query = target.value;
        
        if (!query) {
            results = [];
            return;
        }

        loading = true;
        try {
            const res = await fetch(`/api/search?q=${encodeURIComponent(query)}`);
            if (res.ok) {
                const data = await res.json();
                results = data.items;
            }
        } finally {
            loading = false;
        }
    }
</script>

<input type="search" value={query} oninput={handleInput} placeholder="検索..." />

{#if loading}
    <p>検索中...</p>
{:else}
    <ul>
        {#each results as item}
            <li>{item.title}</li>
        {/each}
    </ul>
{/if}
```

---

## 4. 用語整理・アーキテクチャ

混乱しやすい用語と推奨構成の整理です。

### 用語

1. **Form Actions (SvelteKit機能)**
   - `+page.server.ts` に定義するサーバー関数。今回の推奨パターン。
2. **formaction (HTML属性)**
   - `<button formaction="?/register">`。1つのフォームで複数のActionを使い分ける時に使用。
3. **Remote Functions (次世代)**
   - SvelteKit 2.27+ (Experimental)。RPCライクに関数を呼べる機能。将来的な統合先。

### 推奨アーキテクチャ: BFF (Backend For Frontend)

Laravel と SvelteKit を併用する場合、**SvelteKit のサーバーサイド (`+page.server.ts`) を BFF と見なす**のがベストプラクティスです。

-   **Browser** -> (Form Actions / use:enhance) -> **SvelteKit Server** -> (Server fetch) -> **Laravel API**
- **メリット:**
  - 認証トークン（HttpOnly Cookie）をSvelteKitサーバー内に隠蔽できる。
  - CORS設定が不要。
  - 将来的なバックエンド移行が容易。

---

## 5. 経営・マネジメント視点での選定理由

技術選定を説明する際のロジックとして活用してください。

### 🅰️ 慎重派（安定稼働・コスト削減・セキュリティ重視）への説明

> **「Form Actions (BFFパターン) を主体にします」**
>
> - **品質:** データの整合性を保つための「手続き（再取得、エラー表示）」をフレームワークが自動で行うため、**人為的ミスによるバグを構造的に防げます**。
> - **セキュリティ:** 認証キーをサーバー間通信に閉じ込められるため、漏洩リスクが最小化されます。
> - **保守性:** Web標準に準拠しており、技術トレンドの変化に強く、長期的に安定します。

### 🅱️ 躍進派（UX・スピード・最新技術重視）への説明

> **「基本はForm Actionsで爆速開発し、重要なUXのみFetchを使います」**
>
> - **開発スピード:** ボイラープレート（定型コード）が3〜4割減るため、機能実装のスピードが上がります。
> - **UX:** 重要な機能（検索やいいね）にはFetchと楽観的UI更新を使い、ネイティブアプリ並みの体験を提供します。
> - **トレンド:** React (Next.js) のServer Actions含め、業界全体が「サーバー回帰」しています。この構成は現代の主流です。

---

## 6. 3年・5年・10年先を見据えたロードマップ

- **3年後 (2028年): Remote Functionsの定着**
  - Form ActionsとFetchの使い分けがよりシームレスになり、RPCスタイルが標準化するでしょう。
  - **対策:** 今 Form Actions で書いておけば、概念が近いため移行は容易です。

- **5年後 (2030年): エッジコンピューティングの標準化**
  - SvelteKitサーバー（BFF）がエッジワーカー（Cloudflare Workers等）で動くのが当たり前になります。
  - **対策:** SvelteKitサーバーをステートレス（セッションをメモリに持たない）に設計しておくことが重要です。

- **10年後 (2035年): アーキテクチャの抽象化**
  - **対策:** 重要なのは「HTTPの仕組み」や「ドメイン境界（どこでデータを区切るか）」の設計です。これは10年後も変わりません。
